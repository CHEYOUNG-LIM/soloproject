spring:
  datasource:
    url: jdbc:mysql://localhost:3306/solo?serverTimezone=Asia/Seoul
    username: root
    password: dlacodud1996
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true